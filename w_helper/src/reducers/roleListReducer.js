const _state = [
    {
        roleId: 0,
        roleName: '',
        isJustice: false
    }
];

export default function (state = _state, action) {
    switch(action.type) {
        default:
            return state;
    }
}